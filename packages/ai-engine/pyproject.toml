[tool.poetry]
name = "ai-engine"
version = "0.1.0"
description = "AI Engine for Smart Contract Analysis"
authors = ["AuditChain Team"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.0"
openai = "^1.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"
pytest-asyncio = "^0.21.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
